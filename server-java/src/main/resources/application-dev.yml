spring:
  datasource:
    url: jdbc:mysql://81.71.75.22:3306/wudong?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: ${DB_PASSWORD:wudong123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 300000
      max-lifetime: 1800000
      connection-timeout: 30000
  data:
    redis:
      host: ${REDIS_HOST:81.71.75.22}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:wudong_redis}
      database: 0
      timeout: 5000ms

logging:
  level:
    com.wudong: DEBUG
    org.hibernate.SQL: DEBUG

# Dev-only JWT secret (production MUST set JWT_SECRET env var)
jwt:
  secret: wudong-dev-jwt-secret-key-must-be-at-least-256-bits-long-for-hs256
